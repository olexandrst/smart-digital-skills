Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1
PyYAML==6.0.2

# --- Опціональні SDK для реальних LLM-провайдерів ---
# У MVP працює мок (LLM_MOCK=1), тож ці пакети не обов'язкові.
# Розкоментуйте відповідний рядок, щоб увімкнути реальний провайдер:
# openai==1.54.4                 # OpenAI API та Azure OpenAI
# google-generativeai==0.8.3     # Google Gemini API
