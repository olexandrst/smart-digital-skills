Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1
PyYAML==6.0.2
APScheduler==3.10.4

# --- SDK реальних LLM-провайдерів ---
# У MVP за замовчуванням працює мок (LLM_MOCK=1); ці пакети потрібні для
# реальних викликів (LLM_MOCK=0) до відповідного провайдера.
openai==1.54.4                   # OpenAI API та Azure OpenAI
google-generativeai==0.8.3       # Google Gemini API

# --- Залежності для скілів-пакетів ---
# Бібліотеки, доступні коду скілів під час виконання (у середовищі застосунку).
openpyxl==3.1.5                  # створення/читання Excel (.xlsx) файлів
