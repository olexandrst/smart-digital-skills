Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1
PyYAML==6.0.2
APScheduler==3.10.4

# --- SDK реальних LLM-провайдерів ---
# У MVP за замовчуванням працює мок (LLM_MOCK=1); цей пакет потрібен для
# реальних викликів (LLM_MOCK=0) до Azure OpenAI та локальних (OpenAI-сумісних) серверів.
openai==1.54.4                   # Azure OpenAI та локальні OpenAI-сумісні сервери

# --- Веб-пошук (онлайн-режим чату) ---
# DuckDuckGo без ключа; за замовчуванням мокуємо (WEB_SEARCH_MOCK=1).
ddgs==9.14.4                     # веб-пошук DuckDuckGo для онлайн-режиму

# --- Залежності для скілів-пакетів ---
# Бібліотеки, доступні коду скілів під час виконання (у середовищі застосунку).
openpyxl==3.1.5                  # створення/читання Excel (.xlsx) файлів
